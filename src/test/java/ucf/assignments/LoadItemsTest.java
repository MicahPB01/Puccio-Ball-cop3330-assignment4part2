package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class LoadItemsTest {

    @Test
    void checkShowAll()   {
        //create custom list
        //create custom  arraylist with items
        //call showAll function passing the custom list
        //if the returning arraylist is equal to the custom arraylist, the test passes
    }

    @Test
    void checkShowComplete()   {
        //create custom list some  completed some incomplete
        //create custom  arraylist with items that are completed
        //call showComplete function passing the custom list
        //if the returning arraylist is equal to the custom arraylist, the test passes
    }

    @Test
    void checkShowIncomplete()   {
        //create custom list some  completed some incomplete
        //create custom  arraylist with items that are incomplete
        //call showIncomplete function passing the custom list
        //if the returning arraylist is equal to the custom arraylist, the test passes
    }

    @Test
    void checkComplete()   {
        //create custom list with incomplete task
        //create string which is equal to passing item but instead has 1 instead of 0
        //call complete function passing the custom list
        //if the return string's last character is 1, the test passes
    }

    @Test
    void checkIncomplete()   {
        //create custom list with complete task
        //create string which is equal to passing item but instead has 1 instead of 0
        //call complete function passing the custom list
        //if the return string's last character is 0, the test passes
    }

}